# MedScribe AI — Changelog (Vasile / OpenClaw Bot)

All changes made by Vasile (AI assistant) since the project was uploaded.

---

## Date: February 27, 2026

### 1. Forgot Password Page — NEW
**File:** `src/app/auth/forgot-password/page.tsx`
- Created new page for password reset requests
- Uses Supabase `resetPasswordForEmail()` API
- Shows confirmation message after sending reset email
- Fully translated (EN + RO)
- Redirect URL set to `/auth/reset-password`

### 2. Reset Password Page — NEW
**File:** `src/app/auth/reset-password/page.tsx`
- Created new page where users land after clicking the email reset link
- Password + confirm password fields with validation (min 8 chars, must match)
- On success: updates password via Supabase and redirects to `/dashboard`
- Fully translated (EN + RO)

### 3. "Forgot Password?" Link on Sign In Page — MODIFIED
**File:** `src/app/auth/signin/page.tsx`
- Added "Forgot password?" link between the password field and the Sign In button
- Links to `/auth/forgot-password`

### 4. Translations — MODIFIED
**File:** `src/lib/i18n/translations.ts`
- Added 12 new translation keys for both EN and RO:
  - `auth.forgotPassword` — "Forgot password?" / "Ai uitat parola?"
  - `auth.resetYourPassword` — "Reset your password" / "Resetează parola"
  - `auth.resetDescription` — Description text for the reset form
  - `auth.sendResetLink` — "Send Reset Link" / "Trimite Link de Resetare"
  - `auth.sending` — "Sending..." / "Se trimite..."
  - `auth.resetEmailSent` — Confirmation message
  - `auth.resetInstructions` — Instructions after email sent
  - `auth.backToSignIn` — "Back to Sign In" / "Înapoi la Autentificare"
  - `auth.setNewPassword` — "Set a new password" / "Setează o parolă nouă"
  - `auth.updatePassword` — "Update Password" / "Actualizează Parola"
  - `auth.passwordUpdated` — Success message
  - `auth.redirecting` — "Redirecting to dashboard..."

---

## Date: March 2, 2026

### 5. Show/Hide Password Toggle on Sign In — MODIFIED
**File:** `src/app/auth/signin/page.tsx`
- Added `showPassword` state variable
- Password input type toggles between `password` and `text`
- Added eye/eye-off SVG icon button inside the password field
- Icon is positioned absolute, right-aligned, with hover effect
- Uses `tabIndex={-1}` to avoid tab-focus disrupting login flow

### 6. Build Fix — Booking Page — MODIFIED
**File:** `src/app/(public)/book/page.tsx`
- **Line 818:** Removed reference to undefined `setBookingDoctor(null)` which caused build failure on Vercel
- Changed from: `onClick={() => { setBookingDoctor(null); setView("booking"); setBookingStep(0); }}`
- Changed to: `onClick={() => { setView("booking"); setBookingStep(0); }}`

---

## Vercel Deployment

### Environment Variables — UPDATED
- Re-added `NEXT_PUBLIC_SUPABASE_URL` to Production + Preview + Development environments (was only on Production, causing build-time embedding failure)
- Re-added `NEXT_PUBLIC_SUPABASE_ANON_KEY` to Production
- Re-added `SUPABASE_SERVICE_ROLE_KEY` to Production
- Force-deployed with `--force` flag to clear build cache

### Supabase Admin Actions
- Reset password for user `d.i.pirjol@gmail.com` via Supabase Admin API (user requested)

---

## Files Summary

| File | Action | Description |
|------|--------|-------------|
| `src/app/auth/forgot-password/page.tsx` | **Created** | Forgot password page |
| `src/app/auth/reset-password/page.tsx` | **Created** | Set new password page |
| `src/app/auth/signin/page.tsx` | **Modified** | Added forgot password link + show/hide password |
| `src/lib/i18n/translations.ts` | **Modified** | 12 new translation keys (EN + RO) |
| `src/app/(public)/book/page.tsx` | **Modified** | Build fix — removed undefined variable |

---

*Document generated by Vasile (OpenClaw AI) — March 2, 2026*
