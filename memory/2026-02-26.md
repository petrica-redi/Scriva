# 2026-02-26 — MedScribe Development Day

## Key Achievements

### HTTPS Setup
- MedScribe now runs on HTTPS (port 3001) using mkcert self-signed certs
- Custom server at `server.mjs` with `key.pem` + `cert.pem`
- Diana accesses via `https://10.211.55.3:3001` (Chrome, needs to accept cert warning)
- Safari doesn't work well with self-signed certs — always use Chrome

### Google Meet Integration
- Created `GoogleMeetEmbed.tsx` component
- Remote consultation mode: doctor creates Meet link, pastes it back, video embeds in MedScribe
- AI transcription + clinical assistant run in parallel with video call
- Google Meet may block iframe — fallback is Meet in separate tab

### Transcript Saving to DB
- Fixed: transcripts now saved to `transcripts` table on "End Consultation"
- Segments (speaker, text, timestamp) + full_text stored
- Consultation metadata updated with mode, segment count, timestamp

### GDPR Compliance (13/19 tasks done + 4 pseudonymization)
**Implemented:**
- Privacy Policy page (`/privacy`) — public, no auth needed
- Terms of Service page (`/terms`) — public
- "Delete My Data" in Settings → Data & Privacy tab (type DELETE to confirm)
- Data deletion API — cascade delete all user data
- Export PDF + JSON — buttons in Settings and Patients page
- Enhanced audit log (`src/lib/audit.ts`)
- Audit log viewer in Settings → Audit Log tab
- Granular consent checkboxes on recording page
- AI Transparency Notice on recording page + AI Assistant Panel
- Data Processors tab in Settings (Deepgram, Anthropic, Supabase with DPA info)

### Pseudonymization (CRITICAL GDPR FEATURE)
- `src/lib/pseudonymize.ts` — strips PII before sending to AI
- Replaces: names, CNP, phone, email, DOB, address, MRN with placeholders
- Integrated in 5 API routes: generate-note, regenerate, regenerate-section, ai/ask, ai/analyze-consultation
- De-pseudonymization restores real names in AI responses
- Audio to Deepgram NOT pseudonymized (raw audio stream)
- Data in Supabase stays real (doctor's own database)

### Patient Detail Page
- Created `/patients/[id]/page.tsx` — full patient file
- 4 tabs: Overview, Consultations, Clinical Notes, Risk Assessment
- "Open Case" buttons on dashboard now link to patient detail page
- Patient names in "Patients at Risk" table are clickable links

### Data Residency Status
- Currently ALL data goes to US:
  - Supabase: AWS us-east-1
  - Deepgram: US servers
  - Anthropic: US servers
- EU migration plan in Notion (Supabase Frankfurt, Deepgram EU endpoint, Anthropic via Bedrock EU)

## Notion Integration
- Token: ntn_b1252725706I7fzoZlFoOjlDFPMPAbwPTpbpilnT6X42t5
- Page ID: 30a04aab13ca80dba64ff51cbb2f9ae1
- Database: "MindCare AI — Demo Task List" (ID: 30a04aab-13ca-81f8-87e1-f499986f98b2)
- 28 GDPR tasks added, 17 marked as Done

## Apple Notes
- Created folder "MedScribe" with note "Pseudonimizare & GDPR Data Flow"
- Contains full documentation of pseudonymization + EU data flow plan

## Remaining GDPR Tasks (To Do)
- Consent withdrawal mechanism
- Data retention policy + auto-delete
- Data retention settings in admin panel
- Human oversight: doctor must approve AI notes before finalizing
- Verify encryption at rest + in transit
- DPIA document
- Migrate Supabase to EU
- Configure Deepgram EU endpoint
- Evaluate/replace Anthropic with EU provider
- Update Privacy Policy with pseudonymization docs

## Technical Notes
- Cache corruption happens frequently — fix with `rm -rf .next` and restart
- Server: `node server.mjs` (custom HTTPS server)
- Start all: `bash /Users/bot/.openclaw/workspace/start-all.sh`
- Diana prefers Chrome for testing (Safari has cert issues)
