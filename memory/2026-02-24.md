# 2026-02-24 — MindCare Development Session

## Diana — #startup Discord channel

### What happened:
- Diana asked me to start/manage the MindCare app (Next.js medical scribe app) throughout the day
- App runs on port 3000, needs cloudflared tunnel for Diana to access remotely
- Created `start.sh` script with nohup for persistent processes: `/Users/bot/.openclaw/workspace/MindCare/start.sh`

### Fixes made:
1. **PostgreSQL** — was stuck with stale postmaster.pid, fixed by killing old process + removing pid file
2. **Seeded demo data** — POST /api/seed (30 patients, 40 consultations, 37 notes, 38 transcripts)
3. **Speaker diarization bug** — note page compared `speaker === "doctor"` (string) but Deepgram returns `speaker: 0` (number). Fixed comparison.
4. **AI Clinical Assistant + Note Generation** — Switched from Mistral Large (broken/expired key) to **Claude Sonnet 4** via direct Anthropic API calls
5. **Romanian transcription** — Language defaulted to "en" regardless of UI locale. Fixed to sync `selectedLanguage` with app locale from `useI18n()`
6. **i18n translations** — Translated New Consultation page, spawned sub-agent to translate ALL remaining pages. Dashboard risk flag descriptions translated with `description_ro` fields.
7. **Second sub-agent launched** to translate settings page, specialties, template names, notification labels

### Current tech stack (MindCare):
- **STT:** Deepgram Nova-3 (EN: nova-3-medical, RO: nova-3)
- **LLM:** Claude Sonnet 4 (analyze-consultation + generate-note)
- **DB:** PostgreSQL local (mindcare database)
- **Framework:** Next.js 15.5.12
- **Auth:** NextAuth + Supabase

### Research saved:
- `MindCare/MEDICAL_LLM_RESEARCH.md` — comprehensive list of medical LLMs + STT with prices
- Recommended combo: Deepgram Nova-3 + Claude Sonnet 4 = ~$0.23/consultation, ~$101/month

### Diana's preferences:
- Wants full Romanian translation when RO is selected
- Interested in medical LLMs specialized for Romanian
- Uses "urși" (pornește/deschide) colloquially — I misunderstood it as "kill" initially 😅
- Calls the app "aplicația" 
- Prefers me to just fix things without too much back-and-forth
