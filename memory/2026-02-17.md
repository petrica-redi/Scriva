# 2026-02-17

## MindCare AI Audit & Demo Mode

Diana asked me to analyze, audit, and fix the MindCare AI project (Next.js + Supabase medical app).

### Key Facts
- **Project**: `/Users/bot/.openclaw/workspace/MindCare/MindCare AI v1/`
- **Purpose**: Local demo only — no real auth needed
- **VM IP**: `10.211.55.3` (Parallels VM, Diana on laptop)
- **Supabase**: `https://fikcyksxqgwpaaxixult.supabase.co`
- **Notion**: Token `ntn_b1252725706I7fzoZlFoOjlDFPMPAbwPTpbpilnT6X42t5`, Page `30a04aab13ca80dba64ff51cbb2f9ae1`
- **Dev server**: port 3000

### Completed
- Full deep audit (score 7/10), 30 tasks created in Notion (P0–P3)
- 5/7 P0 bugs fixed: templates crash, new consultation crash, analytics/calendar data leak, Deepgram key exposure, rate limiting added
- Auth bypass: middleware returns `NextResponse.next()`, layout falls back to demo user
- Created `src/lib/utils/demo-auth.ts` — central helper (tries real auth → demo user fallback)
- Applied demo auth to: `/api/consultations`, `/api/consultations/[id]`, `/api/patients`
- Gateway configured: `exec.ask: "off"`, `exec.security: "full"`

### Still TODO
- Apply demo auth to ~9 more API routes: `/api/patients/[id]`, `/api/generate-note`, `/api/deepgram/key`, `/api/deepgram/transcribe`, `/api/search`, `/api/notes/regenerate`, `/api/notes/regenerate-section`, `/api/ai/analyze-consultation`, `/api/templates`
- Diana needs to rotate exposed API keys (Deepgram, Anthropic)
- P1 tasks: billing API, services implementation, audit logging, full-text search, fix /templates/new/edit 404, settings overwrite bug
- Update Notion after completing demo auth

### API Keys (EXPOSED — need rotation)
- Deepgram: `4e07227...` in `.env.local`
- Anthropic: `sk-ant-api03-SUWNGaor-...` in `.env.local`

---

## Evening Session — Supabase → Local PostgreSQL Migration

### What happened
- Original Supabase account inaccessible → decided to replace with local PostgreSQL 17
- Docker/Colima won't work in Parallels VM (no nested virtualization)
- Installed PostgreSQL 17 via Homebrew, created `mindcare` database

### Architecture
- **`pg-adapter.ts`** (`src/lib/supabase/pg-adapter.ts`): Supabase-compatible query builder translating `.from().select().eq().order()` chains → raw SQL. Supports select, insert, update, delete, ilike, in, gte, lte, is, neq, order, limit, single, maybeSingle.
- **`server.ts`** rewritten to use pg-adapter instead of `@supabase/ssr`
- **`client.ts`** rewritten: browser queries proxy through `/api/db` endpoint
- **`/api/db/route.ts`**: server-side proxy for browser DB queries
- **`local-schema.sql`** (`supabase/local-schema.sql`): all tables without RLS or Supabase auth deps
- Demo user ID: `00000000-0000-0000-0000-000000000000`

### Seed data
- 30 psychiatric patients, 40+ consultations (past + future), 37 clinical notes, 38 transcripts
- Realistic Romanian names, ICD-10 psych codes, CPT codes, varied risk levels
- Doctor: "Dr. Maria Ionescu — Psihiatrie"
- Seed cleans demo user data before inserting

### Status at end of session
- ✅ Dashboard fully working with local PostgreSQL
- ✅ Demo auth applied to ALL 12 API routes
- ✅ Database seeded with comprehensive psychiatric data
- ⏳ Sub-pages not yet verified (analytics, calendar, patients, templates, individual consultations)
- ⏳ Notion task list not yet updated with DB migration completion
- Dev server running on port 3000 (session `quiet-trail`, pid 30934)

### Remaining P0 tasks in Notion
- ENV variables in repo (need `.gitignore` fix)
- API key rotation (Diana must do manually)

### TODO for 2026-02-18
- **Diana changing ISP + location** — VM IP will change. When she asks in the morning:
  1. Run `ipconfig getifaddr en0` (or check network interfaces) to get new VM IP
  2. Ensure HTTPS server is running (`start.sh` with auto-restart)
  3. Give her `https://<NEW-IP>:3000`
  4. She'll need to accept the self-signed cert again in Safari
- Server runs via `start.sh` (auto-restart wrapper) in session `clear-haven`
- HTTPS with self-signed cert at `certs/key.pem` / `certs/cert.pem` (CN=10.211.55.3)
  - May need to regenerate cert with new IP in SAN if Safari complains
